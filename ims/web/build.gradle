group "com.aeg.ims"
version "1.0-SNAPSHOT"

apply plugin: "base"
apply plugin: "war"

dependencies {

    compile project(":ims:api"), project(":common"), project(":domain")

    providedCompile(
            ["javax.servlet:javax.servlet-api:$servletVersion"]
    )
}
